/** 
 * GENERATED FILE PLACEHOLDER
 * --------------------------------------------
 * Replace this file by running the Supabase CLI:
 *   npx supabase gen types typescript \
 *     --project-id <YOUR_SUPABASE_PROJECT_REF> \
 *     --schema public > types/supabase.ts
 *
 * Commit the generated file so Vercel builds succeed without the CLI.
 */
// Temporary minimal export to keep TS happy before generation.
// Do NOT rely on this in production; generate real types as above.
export type Database = Record<string, never>;
